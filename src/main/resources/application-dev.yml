logging:
  level:
    com.fpm.registry: trace

spring:
  jpa:
    hibernate.ddl-auto: update
  datasource:
    url: jdbc:mysql://localhost:3306/registry?useSSL=false
    username: spring
    password: spring