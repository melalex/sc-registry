logging:
  level:
    com.fpm.registry: trace
    org.springframework.web: debug
    org.hibernate: info

spring:
  jpa:
    hibernate.ddl-auto: update
  datasource:
    url: jdbc:mysql://localhost:3306/registry?useSSL=false
    username: spring
    password: spring
    platform: mysql